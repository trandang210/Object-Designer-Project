/**
 * An icon to use in a button
 * @class
 */
class Icon {
  /**
   * Constructs an icon with default location
   * @constructs
   * @property {number} x the x coordinate
   * @property {number} y the y coordinate
   */
  constructor () {
    this.x = 0
    this.y = 0
  }

  /**
   * Draws the icon
   */
  draw () {}
}
